# Socrate Desktop Frontend

This is the React + Vite frontend used by the Tauri desktop app.

Run from repository root:

```bash
bash scripts/dev.sh
bash scripts/tauri-dev.sh
bash scripts/package-tauri.sh
```

Direct frontend commands:

```bash
npm install
npm run dev
npm run build
npm run lint
```
